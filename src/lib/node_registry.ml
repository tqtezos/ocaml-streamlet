  let register_node (_: Node.t) : unit = assert false

  let list_nodes (_: unit) : (Node.t list) = assert false

  let from_id (_: string): (Node.t) = assert false

  let start_network = assert false
