open Streamlet_node

  let register_node (_: node_info) : unit = assert false

  let list_nodes (_: unit) : (node_info list) = assert false

  let from_id (_: string): (node_info) = assert false

  let start_network = assert false
